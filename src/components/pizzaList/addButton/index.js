import React from "react";
import { AddButtonWrapper, AddButton } from "./style";

export default () => {
  return (
    <AddButtonWrapper>
      <AddButton>ADD NEW PIZZA</AddButton>
    </AddButtonWrapper>
  );
};
